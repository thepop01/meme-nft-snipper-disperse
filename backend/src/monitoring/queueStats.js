export class QueueStats { constructor() { this.counts = { evictions: 0, provisionalUpgrades: 0, provisionalExpiries: 0 }; } evicted(n = 1) { this.counts.evictions += n; } transition(from, to) { if (from === 'provisional' && to === 'qualified') this.counts.provisionalUpgrades++; if (from === 'provisional' && to === 'expired') this.counts.provisionalExpiries++; } snapshot() { return { ...this.counts }; } }
