export function applyLifecycle(token, { now, cfg, mon }) { const out = { ...token, changed: false, reason: null }; const change = (admission, reason) => Object.assign(out, { admission, changed: admission !== token.admission, reason }); if (token.blockers?.length) return change('rejected', `invalidated:${token.blockers[0].code}`); if (token.admission === 'provisional' && now - token.admissionSince >= cfg.admission.provisional.ttlMs) return change('expired', 'provisional_ttl_elapsed'); if (token.admission === 'watching' && now - token.firstSeenTs >= mon.horizonMs) return change('expired', 'horizon_elapsed'); return out; }
