---description: Search project memoryagent: build---
Search long-term project memory and show relevant memories with their type tags.

Use the `hindsight` MCP `recall` tool (bank for this project) with $ARGUMENTS as the query.
Show each hit as: [TYPE] content (importance, entity). If nothing relevant exists, say so plainly.
